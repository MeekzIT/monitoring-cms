import { REGISTRATE_ORDER } from "../types";

const initialState = {
  registratedData: null,
};

export const subscribeReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
